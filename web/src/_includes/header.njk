<header class="site">
  <div class="wrap nav">
    <a class="brand" href="{{ '/' | url }}">
      <img src="{{ '/assets/images/apple-touch-icon1.png' | url }}" width="1400" height="650" alt="FIT4WOMEN">
    </a>
    <ul class="menu" id="f4w-menu">
      {%- for n in site.nav %}
      <li><a class="{{ 'active' if n.key == active }}" href="{{ n.href | url }}">{{ n.label }}</a></li>
      {%- endfor %}
    </ul>
    <div class="nav-cta">
      <a class="btn btn-primary btn-sm" href="{{ site.primaryCta.href | url }}">{{ site.primaryCta.label }}</a>
      <button class="burger" id="f4w-burger" aria-label="Menu" aria-controls="f4w-menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</header>
